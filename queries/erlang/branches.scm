[
  (if_expr)
  (if_clause)
  (case_expr)
  (cr_clause)
  (catch_clause)
  (catch_expr)
  (try_expr)
  (maybe_expr)
  (receive_expr)
  (receive_after)
] @branch

(binary_op_expr "andalso") @branch
(binary_op_expr "orelse") @branch
