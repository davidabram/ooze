(function_definition
  (signature
    (call_expression
      .
      (identifier) @fn.name))) @fn.def

(function_definition
  (signature
    (identifier) @fn.name)) @fn.def

(arrow_function_expression) @fn.def

(macro_definition
  (signature
    (call_expression
      .
      (identifier) @fn.name))) @fn.def

(macro_definition
  (signature
    (identifier) @fn.name)) @fn.def
